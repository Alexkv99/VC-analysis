[project]
name = "vc-analysis"
version = "0.0.2"
requires-python = ">=3.11"
dependencies = [
  "mcp",
  "uvicorn",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
packages = []
